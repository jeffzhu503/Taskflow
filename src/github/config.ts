// When using the Vite proxy (server.proxy in vite.config.ts), leave this empty.
// For direct calls (no proxy), set to 'http://localhost:8000'.
export const BACKEND_BASE_URL = '';

export const REPO_STORAGE_KEY = 'gh_repo'; // stores "owner/repo"
